{
  "name": "azha-lanchpad",
  "version": "1.0.0",
  "description": "AZHA Lanchpad",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required for static assets.\"",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  }
}
